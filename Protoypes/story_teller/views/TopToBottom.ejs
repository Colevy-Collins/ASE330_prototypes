<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/UI.css">
    <title>Story Webpage</title>
</head>
<body>
    <div class="container">
        <% for (let i = 0; i < answersList.length; i++) { %>
            <div class="prompt">
                <p><%= storyNodes[answersList[i]].question %></p>
            </div>
        <% } %>
        <% if (!storyNodes[currentNode].yesNode && !storyNodes[currentNode].noNode) { %>
            <p>The end.</p>
        <% } else { %>
            <div class="buttons">
                <form action="/answer/yes" method="get">
                    <button id="yesButton" type="submit" <% if (!storyNodes[currentNode].yesNode) { %> disabled <% } %>>Yes</button>
                </form>
                <form action="/answer/no" method="get">
                    <button id="noButton" type="submit" <% if (!storyNodes[currentNode].noNode) { %> disabled <% } %>>No</button>
                </form>
            </div>
        <% } %>
    </div>
</body>
</html>
